# Suggested Commands
- **Unity Editor:** The primary way to run, debug, and build the project.
- **Tests:** Unity Test Framework is installed. Tests can be run via the Test Runner in the Editor.
- **IDE:** Rider or Visual Studio (suggested by packages).
